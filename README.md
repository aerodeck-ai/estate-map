# Aerodeck Estate Map

This repo is generated from the Aerodeck connection-registry. The registry remains the source of truth; `MAP.md` is the human-readable lobby and `registry.json` is the machine-readable export.

Generator: `/home/henry/work/infra/aerodeck-registry/aero-topology-export`
